<%= form_for(@sync_page) do |f| %>
  <% if @sync_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sync_page.errors.count, "error") %> prohibited this sync_page from being saved:</h2>

      <ul>
      <% @sync_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
